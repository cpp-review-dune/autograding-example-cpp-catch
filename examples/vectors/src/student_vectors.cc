#include "student_vectors.hh"

void print_vec(const std::vector<int>& vec)
{
    for (auto x: vec) {
         std::cout << ' ' << x;
    }
    std::cout << '\n';
}

float norm_2(const std::vector<int>& vec)
{
    return 5; // TODO: 
}